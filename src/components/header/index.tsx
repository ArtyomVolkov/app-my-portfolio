import React from 'react';

import Breadcrumbs from '@shared/components/breadcrumbs';

import './style.scss';

const Header = () => (
  <header>
    <Breadcrumbs />
  </header>
);

export default Header;